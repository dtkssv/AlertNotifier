# AlertNotifier
Десктопное приложение для получения алертов из Grafana/AlertManager. 


🚨 AlertNotifier Desktop
AlertBridge Desktop — это настольное приложение-сигнализатор для инженеров (DevOps/SRE), которые хотят получать критические алерты из Grafana/AlertManager прямо на рабочий стол. Никаких браузеров, никаких пропущенных уведомлений — только громкий звук и четкий интерфейс, когда что-то горит.

Проект состоит из двух частей:

Сервер-посредник (Bridge): Легковесный Docker-контейнер, который принимает вебхуки от AlertManager и рассылает их клиентам через WebSocket.
Десктопный клиент: Приложение с графическим интерфейсом (на базе Flet), которое подключается к серверу, показывает активные алерты и издает звуки.
https://link_to_screenshot_or_gif.gif <!-- Добавьте позже ссылку на скриншот или гифку -->

✨ Возможности

🚨 Мгновенные оповещения: Получайте алерты в реальном времени через WebSocket.
🔊 Звуковая сигнализация: Настраиваемые звуки для новых (firing) и разрешенных (resolved) алертов.
🖥️ Удобный интерфейс:
      Темная тема (бережет глаза дежурного).
      Цветовая индикация уровней серьезности (Critical, High, Warning).
      Таблица с детальной информацией по каждому алерту.
📊 Живая статистика: Счетчик активных и критичных алертов всегда перед глазами.
⚙️ Простое подключение: Укажите адрес вашего Bridge-сервера и нажмите "Подключиться".
🐳 Готов к продакшну: Серверная часть упакована в Docker для легкого развертывания на любом Linux-сервере.
🔐 Безопасность: Поддержка Bearer-токена для аутентификации клиентов и вебхуков (опционально).
🏗️ Архитектура
<img width="751" height="335" alt="image" src="https://github.com/user-attachments/assets/f3704365-5fc8-41c6-95b7-ca495f81d625" />


AlertNotifier настроен на отправку вебхуков на наш Bridge-сервер.
Bridge-сервер принимает алерт, сохраняет его в памяти и рассылает всем подключенным клиентам.
Десктопные клиенты (может быть несколько) получают алерт и отображают его.

🚀 Быстрый старт
1. Запуск Bridge-сервера (на вашей Linux VM)
Самый простой способ — использовать Docker Compose.

bash
# Клонируем репозиторий
git clone https://github.com/yourusername/alertbridge-desktop.git
cd alertbridge-desktop

# Переходим в директорию с сервером
cd server

# Создаем файл с переменными окружения (отредактируйте под себя)
cp .env.example .env

# Запускаем контейнер
docker-compose up -d

# Проверяем, что все работает
curl http://localhost:8080/health
Сервер будет доступен по адресу http://<IP_ВАШЕЙ_VM>:8080 для вебхуков и ws://<IP_ВАШЕЙ_VM>:8081/ws для клиентов.

2. Настройка AlertManager
Добавьте в конфиг вашего AlertManager (alertmanager.yml) новый receiver:

yaml
route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'alertbridge'  # Название может быть любым

receivers:
- name: 'alertbridge'
  webhook_configs:
  - url: 'http://<IP_ВАШЕЙ_VM>:8080/webhook'
    send_resolved: true   # Рекомендуем отправлять и события о разрешении
    
3. Запуск Desktop-клиента (на вашем рабочем компьютере)
Требования: Python 3.8+

bash
# Переходим в директорию с клиентом
cd ../client

# Устанавливаем зависимости
pip install -r requirements.txt

# Запускаем приложение (положите свои звуковые файлы в эту же папку)
python main.py
В открывшемся окне введите адрес вашего Bridge-сервера (например, ws://192.168.1.100:8081/ws) и нажмите "Подключиться".

⚙️ Конфигурация
Сервер (Bridge)
Основные параметры настраиваются через переменные окружения в файле .env:

Переменная	Описание	По умолчанию
HTTP_PORT	Порт для входящих вебхуков	8080
WS_PORT	Порт для WebSocket-клиентов	8081
HOST	Интерфейс для прослушивания	0.0.0.0
LOG_LEVEL	Уровень логирования (debug, info)	info
ENABLE_AUTH	Включить аутентификацию по токену	false
AUTH_TOKEN	Секретный токен для доступа	-
MAX_ALERTS	Макс. кол-во алертов в памяти	1000
MAX_CONNECTIONS	Макс. кол-во подключенных клиентов	100
Клиент (Desktop)
Настройки приложения (адрес сервера) сохраняются в файле settings.json в директории программы.

🛠️ Стек технологий
Сервер (Bridge):
Python 3.11
FastAPI (HTTP-эндпоинты)
WebSockets (библиотека websockets)
Uvicorn (ASGI-сервер)
Docker / Docker Compose
Клиент (Desktop):
Python 3.8+
Flet (GUI на базе Flutter)
WebSocket-client
Playsound (для звуков)

📁 Структура проекта

alertbridge-desktop/
├── server/                      # Bridge-сервер
│   ├── app/
│   │   └── main.py              # Основной код сервера
│   ├── Dockerfile               
│   ├── docker-compose.yml
│   ├── requirements.txt
│   └── .env.example              # Пример конфига
├── client/                       # Desktop-клиент
│   ├── main.py                   # GUI приложение
│   ├── requirements.txt
│   ├── alert.wav                  # Звук нового алерта
│   └── resolved.wav               # Звук разрешения
└── README.md                     # Этот файл

📄 Лицензия
Проект распространяется под лицензией MIT. Подробности в файле LICENSE.

🙏 Благодарности
Проект создан для упрощения жизни дежурным инженерам.
Вдохновлен необходимостью иметь "красную лампочку" на столе, когда ломается продакшн.
Если проект оказался полезным, поставьте звездочку на GitHub ⭐ Это мотивирует развивать его дальше!
